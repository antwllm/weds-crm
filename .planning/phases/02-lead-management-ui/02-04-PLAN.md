---
phase: 02-lead-management-ui
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - client/src/hooks/useActivities.ts
  - client/src/components/leads/LeadDetail.tsx
  - client/src/components/leads/InlineField.tsx
  - client/src/components/leads/LeadForm.tsx
  - client/src/components/leads/ActivityTimeline.tsx
  - client/src/components/leads/NoteInput.tsx
  - client/src/pages/LeadDetailPage.tsx
  - client/src/pages/LeadFormPage.tsx
autonomous: true
requirements:
  - LEAD-01
  - LEAD-04
  - LEAD-05
  - LEAD-07
  - LEAD-08

must_haves:
  truths:
    - "William can create a lead from a form with name, email, phone, event date, source, budget, message"
    - "William can click a field on the lead detail page and edit it inline (Notion-style)"
    - "Inline edits auto-save on blur or Enter key press"
    - "William can add a timestamped note that appears in the activity timeline"
    - "Activity timeline shows emails, notes, and status changes in chronological order, color-coded"
    - "William can delete a lead from the detail page with a confirmation dialog"
  artifacts:
    - path: "client/src/components/leads/LeadDetail.tsx"
      provides: "Two-column layout: editable fields left, timeline + notes right"
    - path: "client/src/components/leads/InlineField.tsx"
      provides: "Click-to-edit field with auto-save on blur/Enter"
    - path: "client/src/components/leads/LeadForm.tsx"
      provides: "Create lead form with validation"
    - path: "client/src/components/leads/ActivityTimeline.tsx"
      provides: "Chronological activity list with color-coded types"
    - path: "client/src/components/leads/NoteInput.tsx"
      provides: "Textarea input for adding notes"
    - path: "client/src/hooks/useActivities.ts"
      provides: "TanStack Query hooks for activities and notes"
  key_links:
    - from: "client/src/components/leads/InlineField.tsx"
      to: "client/src/hooks/useLeads.ts"
      via: "useUpdateLead mutation on save"
      pattern: "onSave.*mutate"
    - from: "client/src/components/leads/NoteInput.tsx"
      to: "/api/leads/:id/notes"
      via: "useCreateNote mutation"
      pattern: "apiFetch.*notes"
    - from: "client/src/components/leads/ActivityTimeline.tsx"
      to: "/api/leads/:id/activities"
      via: "useActivities query"
      pattern: "apiFetch.*activities"
---

<objective>
Build the lead detail page with inline editing, activity timeline, note input, lead creation form, and lead deletion.

Purpose: This is where William manages individual leads -- editing their data, tracking all interactions, and adding notes. Combined with the Kanban board, this completes the core CRM workflow.
Output: Lead detail page with two-column layout, inline editing, activity timeline, note creation, and lead form for manual creation.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lead-management-ui/02-CONTEXT.md
@.planning/phases/02-lead-management-ui/02-RESEARCH.md
@.planning/phases/02-lead-management-ui/02-01-SUMMARY.md
@.planning/phases/02-lead-management-ui/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From client/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

From client/src/lib/constants.ts:
```typescript
export const PIPELINE_STAGES: Array<{ value: string; label: string; color: string }>;
export const ACTIVITY_TYPE_LABELS: Record<string, { label: string; color: string; icon: string }>;
```

<!-- From Plan 02 (backend API) -->
API endpoints:
- GET /api/leads/:id -> Lead (single lead - to be added if not in plan 02, or filter client-side)
- PATCH /api/leads/:id { ...fields } -> Lead
- DELETE /api/leads/:id -> 204
- GET /api/leads/:id/activities -> Activity[]
- POST /api/leads/:id/notes { content: string } -> Activity

<!-- From Plan 03 (shared hooks) -->
From client/src/hooks/useLeads.ts:
```typescript
export function useLeads(filters?: LeadFilters): UseQueryResult<Lead[]>;
export function useUpdateLead(): UseMutationResult;
export function useDeleteLead(): UseMutationResult;
export function useCreateLead(): UseMutationResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InlineField, ActivityTimeline, NoteInput, and useActivities hook</name>
  <files>
    client/src/hooks/useActivities.ts, client/src/components/leads/InlineField.tsx,
    client/src/components/leads/ActivityTimeline.tsx, client/src/components/leads/NoteInput.tsx
  </files>
  <action>
    1. Create client/src/hooks/useActivities.ts:
       - useActivities(leadId: number): useQuery for GET /api/leads/:id/activities. Query key: ['activities', leadId].
       - useCreateNote(leadId: number): useMutation for POST /api/leads/:id/notes. On success: invalidate ['activities', leadId].

    2. Create client/src/components/leads/InlineField.tsx:
       - Props: value (string), onSave (newValue: string) => void, label (string), type ('text' | 'email' | 'tel' | 'date' | 'number' | 'textarea' | 'select'), placeholder (string), options (for select type: {value, label}[])
       - Display mode: shows label (text-sm muted) + value (or dash for empty). Entire area is clickable with hover:bg-muted rounded transition.
       - Edit mode: renders shadcn Input (or Textarea for textarea type, or Select for select type) with autoFocus
       - Saves on blur or Enter key. Escapes on Escape key (reverts to original value).
       - If draft !== original value on save, calls onSave. Otherwise just exits edit mode.
       - For number type: parse as number before saving

    3. Create client/src/components/leads/ActivityTimeline.tsx:
       - Accepts activities: Activity[] (already sorted chronologically from API)
       - Renders vertical timeline with connecting line
       - Each activity: icon (from ACTIVITY_TYPE_LABELS mapped to lucide-react icons), colored dot, type label, content, relative timestamp (date-fns formatDistanceToNow with fr locale, addSuffix: true)
       - For status_change: show "Statut: {from} -> {to}" extracted from metadata
       - For note_added: show the note content
       - For email types: show content/snippet
       - Empty state: "Aucune activite" message

    4. Create client/src/components/leads/NoteInput.tsx:
       - shadcn Textarea + "Ajouter" Button
       - Textarea placeholder: "Ajouter une note..."
       - On submit: call useCreateNote mutation, clear textarea on success, show toast.success("Note ajoutee")
       - Disable button when textarea is empty or mutation is pending
       - Show loading state on button during mutation
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>InlineField supports click-to-edit with auto-save. ActivityTimeline renders chronological activities with color-coded icons. NoteInput creates notes via API. All components compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build LeadDetail, LeadForm, LeadDetailPage, and LeadFormPage</name>
  <files>
    client/src/components/leads/LeadDetail.tsx, client/src/components/leads/LeadForm.tsx,
    client/src/pages/LeadDetailPage.tsx, client/src/pages/LeadFormPage.tsx
  </files>
  <action>
    1. Create client/src/components/leads/LeadDetail.tsx:
       - Two-column layout per user decision: left column = editable fields, right column = activity timeline + note input
       - Left column fields using InlineField:
         * Nom (text, required)
         * Email (email)
         * Telephone (tel)
         * Date de l'evenement (text -- keep as free-form since existing data uses varchar)
         * Budget (number, formatted as EUR)
         * Source (select, with SOURCE_BADGES options + "Manuel" option)
         * Statut (select, with PIPELINE_STAGES options)
         * Message (textarea, read-only display or editable)
       - Each InlineField onSave calls useUpdateLead mutation with { [fieldName]: newValue }
       - Show toast.success("Modifications enregistrees") on successful save
       - Show toast.error on failure
       - Right column: ActivityTimeline (useActivities) + NoteInput below it
       - Responsive: on mobile (below lg breakpoint), stack columns vertically (fields on top, timeline below)
       - Delete button at bottom of left column: red outline button "Supprimer ce lead"

    2. Create client/src/components/leads/LeadForm.tsx:
       - Form for creating a new lead (POST /api/leads)
       - Fields: Nom (required), Email, Telephone, Date evenement, Source (select defaulting to "manuel"), Budget, Message (textarea)
       - Use shadcn Input, Select, Textarea, Button components
       - Client-side validation: name is required (show inline error "Le nom est requis")
       - On submit: call useCreateLead mutation
       - On success: toast.success("Lead cree"), navigate to /leads/:newId
       - On error: toast.error with server message
       - "Annuler" button navigates back to /pipeline

    3. Create client/src/pages/LeadDetailPage.tsx:
       - Gets leadId from useParams
       - Fetches lead using useLeads() and finds by id (or add a useLead(id) hook that fetches GET /api/leads and filters -- keep simple since it's a single-user CRM with limited data)
       - If lead is not found: show "Lead non trouve" with link back to pipeline
       - Header: back arrow (link to /pipeline) + lead name + delete button
       - Delete flow: shadcn AlertDialog with "Supprimer ce lead?" title, "Cette action est irreversible." description, "Annuler" and "Supprimer" buttons
       - On delete confirm: useDeleteLead mutation, navigate to /pipeline on success
       - Renders LeadDetail component with the lead data

    4. Create client/src/pages/LeadFormPage.tsx:
       - Simple page wrapper with "Nouveau lead" title + back link to /pipeline
       - Renders LeadForm component

    5. Update client/src/App.tsx: replace "/leads/:id" placeholder with LeadDetailPage and "/leads/new" with LeadFormPage. IMPORTANT: "/leads/new" route must be defined BEFORE "/leads/:id" so React Router matches it first.

    All text in French. All fields use French labels.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Lead detail page renders two-column layout with inline editing and activity timeline. Lead form creates new leads. Delete confirmation dialog works. All routes properly connected.</done>
</task>

</tasks>

<verification>
- `cd client && npx vite build` succeeds
- Lead detail page shows two-column layout
- InlineField edits save on blur/Enter
- Activity timeline shows chronological entries with icons
- Note input creates notes and refreshes timeline
- Lead form validates and creates leads
- Delete confirmation dialog removes leads
</verification>

<success_criteria>
- Lead detail page has editable fields on left, timeline + notes on right
- Inline editing saves on blur/Enter (Notion-style)
- Activity timeline shows all types color-coded in chronological order
- Notes can be added and appear immediately in timeline
- New lead form validates name, creates lead, navigates to detail
- Delete flow uses confirmation dialog, removes lead, returns to pipeline
- Responsive: stacks vertically on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-management-ui/02-04-SUMMARY.md`
</output>
