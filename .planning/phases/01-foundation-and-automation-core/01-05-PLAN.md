---
phase: 01-foundation-and-automation-core
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - src/pipeline/process-email.ts
  - src/pipeline/scheduler.ts
  - src/services/pubsub.ts
  - src/routes/webhook.ts
  - tests/pipeline.test.ts
  - tests/duplicate.test.ts
autonomous: false
requirements:
  - PARS-02
  - LEAD-09

user_setup:
  - service: gcp-pubsub
    why: "Gmail push notifications require a Pub/Sub topic and subscription"
    env_vars:
      - name: GMAIL_PUBSUB_TOPIC
        source: "GCP Console -> Pub/Sub -> Topics -> Create topic (e.g., projects/YOUR_PROJECT/topics/gmail-notifications)"
    dashboard_config:
      - task: "Create Pub/Sub topic for Gmail push notifications"
        location: "GCP Console -> Pub/Sub -> Topics"
      - task: "Create push subscription pointing to Cloud Run URL /webhook/gmail"
        location: "GCP Console -> Pub/Sub -> Subscriptions"
      - task: "Grant Gmail publish permission: gmail-api-push@system.gserviceaccount.com as Publisher on the topic"
        location: "GCP Console -> Pub/Sub -> Topic -> Permissions"
  - service: gcs
    why: "vCard file hosting"
    env_vars:
      - name: GCS_BUCKET_NAME
        source: "GCP Console -> Cloud Storage -> Create bucket"
    dashboard_config:
      - task: "Create a Cloud Storage bucket for vCard files"
        location: "GCP Console -> Cloud Storage -> Buckets"
  - service: twilio
    why: "Prospect SMS sending"
    env_vars:
      - name: TWILIO_ACCOUNT_SID
        source: "Twilio Console -> Account SID"
      - name: TWILIO_AUTH_TOKEN
        source: "Twilio Console -> Auth Token"
      - name: TWILIO_PHONE_NUMBER
        source: "Twilio Console -> Phone Numbers -> Buy a number (with SMS capability)"
  - service: free-mobile
    why: "Admin SMS notifications"
    env_vars:
      - name: FREE_MOBILE_USER
        source: "Free Mobile account -> Mon Compte -> Mes Options -> Notifications par SMS"
      - name: FREE_MOBILE_PASS
        source: "Free Mobile account -> Mon Compte -> Mes Options -> Notifications par SMS -> Cle d'identification"

must_haves:
  truths:
    - "Full pipeline processes an email end-to-end: parse -> dedup check -> create lead -> generate vCard -> upload -> notify"
    - "Duplicate leads (same email or phone) are detected and logged as activity on existing lead"
    - "Pub/Sub webhook receives Gmail push notification and triggers pipeline"
    - "Fallback cron sweep runs on schedule and catches missed emails"
    - "Concurrency guard prevents overlapping pipeline runs"
    - "Gmail watch is renewed on startup and daily"
  artifacts:
    - path: "src/pipeline/process-email.ts"
      provides: "Full email processing pipeline: parse, dedup, create lead, vCard, notify"
      exports: ["processOneEmail", "processPendingEmails"]
    - path: "src/pipeline/scheduler.ts"
      provides: "Cron-based fallback sweep and Gmail watch renewal"
      exports: ["startScheduler"]
    - path: "src/services/pubsub.ts"
      provides: "Pub/Sub message handler that triggers pipeline"
      exports: ["handlePubSubMessage"]
    - path: "src/routes/webhook.ts"
      provides: "POST /webhook/gmail endpoint for Pub/Sub push"
  key_links:
    - from: "src/pipeline/process-email.ts"
      to: "src/services/parser.ts"
      via: "parseMarriagesNetEmail call"
      pattern: "parseMarriagesNetEmail"
    - from: "src/pipeline/process-email.ts"
      to: "src/services/notifications.ts"
      via: "dispatchNotifications call after lead creation"
      pattern: "dispatchNotifications"
    - from: "src/pipeline/process-email.ts"
      to: "src/db/schema.ts"
      via: "Drizzle insert into leads and activities tables"
      pattern: "db\\.insert\\(leads\\)"
    - from: "src/routes/webhook.ts"
      to: "src/pipeline/process-email.ts"
      via: "Webhook triggers processPendingEmails"
      pattern: "processPendingEmails"
    - from: "src/pipeline/scheduler.ts"
      to: "src/pipeline/process-email.ts"
      via: "Cron triggers processPendingEmails"
      pattern: "processPendingEmails"
---

<objective>
Wire the full email processing pipeline: Pub/Sub webhook, cron scheduler, and the orchestrator that ties together parsing, dedup, lead creation, vCard generation, and notification dispatch.

Purpose: This is the final assembly -- connecting all services built in Plans 02-04 into the end-to-end automated pipeline. When a Mariages.net email arrives, the system should parse it, check for duplicates, create a lead, generate and upload a vCard, and fire all three notifications without human intervention.
Output: Complete headless automation pipeline with Pub/Sub trigger, cron fallback, and concurrency protection.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-automation-core/01-CONTEXT.md
@.planning/phases/01-foundation-and-automation-core/01-RESEARCH.md
@.planning/phases/01-foundation-and-automation-core/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-automation-core/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-automation-core/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-automation-core/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD pipeline orchestrator and duplicate detection</name>
  <files>src/pipeline/process-email.ts, tests/pipeline.test.ts, tests/duplicate.test.ts</files>
  <behavior>
    - processOneEmail(gmailMessageId, gmailClient) processes a single email end-to-end
    - Pipeline: getMessageContent -> parseMarriagesNetEmail -> checkDuplicate -> createLead -> generateVCard -> uploadVCard -> dispatchNotifications -> modifyLabels
    - On parse failure: label email with weds-crm/error, log error, send SMS alert to admin, continue
    - On duplicate: log activity on existing lead, label as processed, skip notifications
    - On success: create lead, create activity (email_received), generate vCard, upload to GCS, store vCardUrl on lead, dispatch notifications, relabel email (add processed, remove pending)
    - processPendingEmails() searches for pending emails and processes each with concurrency guard
    - Concurrency guard: if already processing, skip silently
    - Duplicate detection matches on normalized email OR normalized phone
  </behavior>
  <action>
RED phase:
1. Create `tests/duplicate.test.ts`:
   - Test: email match -> returns { isDuplicate: true, existingLeadId }
   - Test: phone match (different email) -> returns { isDuplicate: true, existingLeadId }
   - Test: no match -> returns { isDuplicate: false }
   - Test: null phone does not match null phone (only match on actual values)
   - Mock Drizzle db queries

2. Create `tests/pipeline.test.ts`:
   - Test "create lead": mock all services, verify lead is inserted into DB with correct fields
   - Test "archive": verify email labels are modified (add processed, remove pending)
   - Test "skip processed": verify concurrency guard prevents overlapping runs
   - Test "duplicate handling": verify activity is logged on existing lead, no new lead created, no notifications sent
   - Test "parse failure": verify email labeled with error, admin SMS sent, pipeline continues
   - Mock: gmail service, parser, notifications, storage, db

3. Run tests -- must FAIL.

GREEN phase:
4. Create `src/pipeline/process-email.ts`:
   - Module-level `isProcessing = false` concurrency guard
   - `checkDuplicate(email: string, phone: string | null): Promise<{ isDuplicate: boolean; existingLeadId?: number }>`
     * Query leads table: WHERE email = lower(email) OR (phone IS NOT NULL AND phone = normalizedPhone)
     * Return first match or { isDuplicate: false }
   - `processOneEmail(messageId: string, gmail: gmail_v1.Gmail, labelIds: Record<string, string>): Promise<PipelineResult>`
     * Get message content
     * Parse with parseMarriagesNetEmail
     * On parse failure: modifyLabels (add error, remove pending), log error, send Free Mobile alert SMS, return early
     * Check duplicate -- if duplicate: insert activity (duplicate_inquiry) on existing lead, modifyLabels (add processed, remove pending), return
     * Insert new lead into DB
     * Insert activity (email_received) on new lead
     * Generate vCard content with generateVCardContent
     * Upload vCard with uploadVCardAndGetSignedUrl, store URL on lead (UPDATE leads SET vCardUrl)
     * Dispatch all notifications via dispatchNotifications
     * Modify Gmail labels: add processed, remove pending
     * Return PipelineResult
   - `processPendingEmails(gmail: gmail_v1.Gmail): Promise<void>`
     * Concurrency guard check
     * Ensure labels exist (ensureLabelsExist)
     * Search for messages with pending label
     * Process each message with processOneEmail
     * Wrap in try/finally to reset isProcessing

5. Run tests -- must PASS.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run tests/pipeline.test.ts tests/duplicate.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Pipeline processes emails end-to-end. Duplicates detected by email or phone. Parse failures handled gracefully. Concurrency guard works. Labels correctly managed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Pub/Sub webhook, cron scheduler, and wire into Express app</name>
  <files>src/services/pubsub.ts, src/pipeline/scheduler.ts, src/routes/webhook.ts, src/app.ts</files>
  <action>
1. Create `src/services/pubsub.ts`:
   - `handlePubSubMessage(messageData: string): { historyId: string, emailAddress: string }` -- decodes base64 Pub/Sub message data and extracts Gmail notification fields
   - Validates that emailAddress matches the configured allowed user email
   - Returns parsed data for the webhook handler to use

2. Create `src/pipeline/scheduler.ts`:
   - `startScheduler(gmail: gmail_v1.Gmail): void`
   - Uses node-cron to schedule two jobs:
     * Fallback sweep: config.fallbackSweepInterval (default '*/30 * * * *') -> calls processPendingEmails(gmail)
     * Gmail watch renewal: '0 3 * * *' (daily at 3 AM) -> calls renewGmailWatch
   - `renewGmailWatch(gmail: gmail_v1.Gmail): Promise<void>` -- calls gmail.users.watch with the configured Pub/Sub topic and label filter. Logs expiration timestamp.
   - Renew watch immediately on scheduler start (not just on cron)
   - Export startScheduler

3. Create `src/routes/webhook.ts`: Express Router with:
   - POST `/webhook/gmail` -- receives Pub/Sub push notification
   - Extracts message.data from req.body, decodes with handlePubSubMessage
   - Calls processPendingEmails (do NOT process based on historyId -- just trigger a full sweep, which is simpler and the concurrency guard protects against redundant runs)
   - Returns 200 immediately (acknowledge Pub/Sub before processing completes to avoid redelivery -- use setImmediate or process.nextTick for async processing)
   - No auth required on this endpoint (Pub/Sub push uses OIDC verification at the Cloud Run level, not in app code)

4. Update `src/app.ts`:
   - Mount webhook route: app.use('/webhook', webhookRouter)
   - After successful OAuth callback, store tokens and initialize Gmail client
   - On app startup (in index.ts): after session is available, check if stored OAuth tokens exist in the database or session, and if so, create Gmail client and start scheduler
   - Add a mechanism to get the authenticated Gmail client for the pipeline (e.g., store in app.locals or a singleton service)

5. Update `src/index.ts`:
   - After server starts, attempt to restore Gmail client from stored refresh token
   - If tokens available: create Gmail client, start scheduler, renew watch
   - If no tokens: log "En attente d'authentification OAuth" (French) -- scheduler starts after first login
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --pretty 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Pub/Sub webhook accepts POST and triggers pipeline. Cron scheduler runs fallback sweep and watch renewal. Gmail watch renewed on startup. App wires everything together. TypeScript compiles. All existing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end headless automation pipeline</name>
  <files>N/A</files>
  <action>
Human verifies the complete headless automation pipeline works end-to-end with real services.

What was built: Complete headless automation pipeline -- Gmail push (Pub/Sub) triggers email parsing, lead creation with dedup, vCard generation and upload to GCS, and independent dispatch of all three notifications (prospect SMS via Twilio, admin SMS via Free Mobile with vCard link, email recap to contact@weds.fr with vCard attachment). Includes cron fallback sweep and concurrency protection.
  </action>
  <verify>
Steps to verify:
1. Run locally: `cp .env.example .env`, fill in credentials, run `npx tsx src/index.ts`
2. Visit http://localhost:8080/health -- should return { status: 'ok' }
3. Visit http://localhost:8080/ -- should redirect to Google OAuth login
4. Complete OAuth login with your Google account
5. Verify in terminal: "Gmail watch renewed" and "Scheduler started" logs
6. Send a test Mariages.net-format email to your Gmail and label it `weds-crm/pending`
7. Wait up to 30s (Pub/Sub push) or trigger manually
8. Verify: lead appears in database, vCard uploaded to GCS bucket, SMS received on phone (both Twilio and Free Mobile with vCard link), email recap received at contact@weds.fr with vCard attachment
9. Send the same email again -- verify duplicate is detected and logged as activity, no new lead created
10. Check Sentry dashboard for any errors

Resume: Type "approved" or describe issues to fix
  </verify>
  <done>Human confirms: lead created in DB, all 3 notifications received, duplicate detection works, no Sentry errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- zero errors
- `npx vitest run` -- all tests pass (parser, vcard, notifications, pipeline, duplicate, auth)
- `docker build -t weds-crm .` -- builds successfully
- Pipeline processes test email end-to-end (manual verification in checkpoint)
- Duplicate detection works for email and phone matches
- All three notifications fire independently
</verification>

<success_criteria>
- Pub/Sub webhook triggers pipeline on Gmail push notification
- Cron fallback sweep catches missed emails every 30 minutes
- Gmail watch renews on startup and daily
- Concurrency guard prevents overlapping pipeline runs
- Full pipeline: parse -> dedup -> create lead -> vCard -> upload -> notify -> relabel
- Duplicate detection on email OR phone prevents duplicate leads
- Parse failures: error label, admin alert, continue processing
- All services wired into Express app with proper auth
- Human verification confirms end-to-end flow works with real services
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-automation-core/01-05-SUMMARY.md`
</output>
