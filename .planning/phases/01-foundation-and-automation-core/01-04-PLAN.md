---
phase: 01-foundation-and-automation-core
plan: 04
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/services/sms.ts
  - src/services/notifications.ts
  - tests/notifications.test.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03

must_haves:
  truths:
    - "Prospect receives a personalized French SMS via Twilio on new lead"
    - "Admin receives SMS via Free Mobile with vCard download link"
    - "contact@weds.fr receives email recap with vCard attachment"
    - "Each notification fires independently -- one failure does not block others"
    - "Failed notifications are logged with Sentry and trigger triple alerting"
  artifacts:
    - path: "src/services/sms.ts"
      provides: "Twilio SMS and Free Mobile SMS sending"
      exports: ["sendTwilioSMS", "sendFreeMobileSMS"]
    - path: "src/services/notifications.ts"
      provides: "Orchestrator dispatching all 3 notifications independently via Promise.allSettled"
      exports: ["dispatchNotifications"]
  key_links:
    - from: "src/services/notifications.ts"
      to: "src/services/sms.ts"
      via: "Calls sendTwilioSMS and sendFreeMobileSMS"
      pattern: "sendTwilioSMS|sendFreeMobileSMS"
    - from: "src/services/notifications.ts"
      to: "src/services/gmail.ts"
      via: "Calls sendEmail for recap"
      pattern: "sendEmail"
    - from: "src/services/notifications.ts"
      to: "Promise.allSettled"
      via: "Independent dispatch pattern"
      pattern: "Promise\\.allSettled"
---

<objective>
SMS services (Twilio + Free Mobile) and the notification orchestrator that dispatches all three notifications independently.

Purpose: When a new lead is created, three notifications must fire in parallel: prospect SMS, admin SMS with vCard link, and email recap with vCard attachment. Failures in one must not block others. This plan builds and tests that dispatch layer.
Output: Tested SMS services and notification orchestrator with independent dispatch pattern.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-automation-core/01-CONTEXT.md
@.planning/phases/01-foundation-and-automation-core/01-RESEARCH.md
@.planning/phases/01-foundation-and-automation-core/01-01-SUMMARY.md

Port from existing code:
@/Users/william/Documents/Development/email-parser/src/services/sms.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD SMS services (Twilio + Free Mobile)</name>
  <files>tests/notifications.test.ts, src/services/sms.ts</files>
  <behavior>
    - sendTwilioSMS(name, eventDate, phone) calls Twilio API with French message "Bonjour {name}, merci..."
    - sendTwilioSMS returns { channel: 'twilio_sms', success: true } on success
    - sendTwilioSMS returns { channel: 'twilio_sms', success: false, error: '...' } on API failure
    - sendFreeMobileSMS(lead, vCardUrl) calls Free Mobile API with lead summary + vCard link
    - sendFreeMobileSMS includes vCard URL in message body
    - sendFreeMobileSMS returns NotificationResult
    - Both services use axios for HTTP calls (mockable)
  </behavior>
  <action>
RED phase:
1. Create `tests/notifications.test.ts`:
   - Mock axios at module level using vi.mock('axios')
   - Test sendTwilioSMS: verify it calls axios.post with correct Twilio URL, Basic auth header (base64 of accountSid:authToken), URLSearchParams body with From, To, Body. Verify French message template includes name and event date.
   - Test sendTwilioSMS error handling: mock axios to reject, verify it returns { success: false, error }
   - Test sendFreeMobileSMS: verify it calls Free Mobile API URL with user, pass, msg params. Verify msg includes lead name, phone, email, event date, message excerpt, and vCard signed URL.
   - Test sendFreeMobileSMS error handling
2. Run tests -- must FAIL.

GREEN phase:
3. Create `src/services/sms.ts`:
   - Port from email-parser/src/services/sms.js to TypeScript
   - `sendTwilioSMS(name: string, eventDate: string, phone: string): Promise<NotificationResult>`
     * POST to `https://api.twilio.com/2010-04-01/Accounts/${config.twilio.accountSid}/Messages.json`
     * Auth: Basic base64(accountSid:authToken)
     * Body: URLSearchParams { From: config.twilio.phoneNumber, To: phone, Body: French message }
     * French message template: "Bonjour {name}, merci d'avoir pris contact avec weds.fr. Nous avons bien recu votre demande de photographe pour votre mariage le {eventDate}. Nous revenons vers vous d'ici 24h.\n\nL'equipe de weds.fr"
   - `sendFreeMobileSMS(lead: { name: string, phone: string, email: string, eventDate: string, message: string }, vCardUrl: string): Promise<NotificationResult>`
     * GET to `https://smsapi.free-mobile.fr/sendmsg` with params: user, pass, msg
     * Message includes: name, event date, phone, email, message excerpt (first 200 chars), vCard URL
4. Run tests -- must PASS.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run tests/notifications.test.ts -t "twilio|free mobile" --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Twilio SMS tests pass with mocked axios. Free Mobile SMS tests pass. Both return NotificationResult. French message templates are correct. vCard URL included in admin SMS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD notification orchestrator with independent dispatch</name>
  <files>tests/notifications.test.ts, src/services/notifications.ts</files>
  <behavior>
    - dispatchNotifications(lead, vCardUrl, vCardContent, gmailClient) fires all 3 notifications
    - If Twilio fails, admin SMS and email recap still succeed
    - If Free Mobile fails, Twilio SMS and email recap still succeed
    - Results array contains status for each channel
    - Failed notifications are logged via logger.error and Sentry
    - Activity records are created for each notification attempt (sms_sent or notification_failed)
  </behavior>
  <action>
RED phase:
1. Add tests to `tests/notifications.test.ts` in a new describe block "dispatchNotifications":
   - Mock sendTwilioSMS, sendFreeMobileSMS, sendEmail (from gmail.ts), logger, and db
   - Test: all succeed -> returns 3 success results
   - Test: Twilio fails -> returns 1 failure + 2 successes (order independent)
   - Test: all fail -> returns 3 failures, all logged
   - Test: activity records created for each notification
2. Run tests -- must FAIL.

GREEN phase:
3. Create `src/services/notifications.ts`:
   - `dispatchNotifications(lead: Lead, vCardUrl: string, vCardContent: string, gmailClient: gmail_v1.Gmail): Promise<NotificationResult[]>`
   - Use `Promise.allSettled` to fire all 3 independently:
     * sendTwilioSMS(lead.name, lead.eventDate, lead.phone)
     * sendFreeMobileSMS({ name, phone, email, eventDate, message }, vCardUrl)
     * sendEmail(gmailClient, config.adminEmail, subject, htmlBody, [{ filename: `${lead.name}.vcf`, content: vCardContent, mimeType: 'text/vcard' }])
   - Email recap subject: `Nouveau lead Mariages.net - ${lead.name}` (French)
   - Email recap body: HTML with lead details (name, email, phone, event date, message, vCard link)
   - For each settled result: log success or error, create activity record (type: 'sms_sent' or 'notification_failed')
   - On failure: call logger.error and Sentry.captureException with channel tag
   - Return array of NotificationResult
4. Run tests -- must PASS.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run tests/notifications.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All notification tests pass. Independent dispatch proven -- one failure does not block others. Activity logging for each notification. French message templates. Email recap includes vCard attachment.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/notifications.test.ts` -- all tests green
- `npx tsc --noEmit` -- zero errors
- Twilio SMS uses correct API endpoint and auth pattern
- Free Mobile SMS includes vCard signed URL
- Email recap has vCard attachment
- Promise.allSettled pattern used (not Promise.all)
- Failures logged to Sentry with channel tags
</verification>

<success_criteria>
- Twilio SMS sends personalized French message to prospect phone number
- Free Mobile SMS sends lead summary with vCard download link to admin
- Email recap sends to contact@weds.fr with vCard file attachment
- All 3 notifications fire independently via Promise.allSettled
- One notification failure does not block the others
- Each notification attempt creates an activity record on the lead
- Failed notifications trigger Sentry error capture
- All message templates are in French
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-automation-core/01-04-SUMMARY.md`
</output>
