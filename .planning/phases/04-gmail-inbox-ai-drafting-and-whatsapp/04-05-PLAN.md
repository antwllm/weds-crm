---
phase: 04-gmail-inbox-ai-drafting-and-whatsapp
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - client/src/pages/SettingsPage.tsx
  - client/src/components/settings/TemplateEditor.tsx
  - client/src/components/settings/AiPromptEditor.tsx
  - client/src/components/settings/WhatsAppTemplates.tsx
  - client/src/hooks/useSettings.ts
  - client/src/components/layout/Sidebar.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - MAIL-05
  - MAIL-06
  - MAIL-07
  - MAIL-08

must_haves:
  truths:
    - "William can create, edit, and delete email templates with variables"
    - "William can manage the AI prompt template in a textarea editor"
    - "Template editor shows variable placeholders with preview"
    - "AI prompt editor has a preview button showing substituted output"
    - "Settings page is accessible from sidebar"
  artifacts:
    - path: "client/src/pages/SettingsPage.tsx"
      provides: "Settings page with Modeles + Parametres IA tabs"
      min_lines: 30
    - path: "client/src/components/settings/TemplateEditor.tsx"
      provides: "Email template CRUD UI"
    - path: "client/src/components/settings/AiPromptEditor.tsx"
      provides: "AI prompt template editor"
  key_links:
    - from: "client/src/hooks/useSettings.ts"
      to: "/api/templates"
      via: "React Query CRUD operations"
      pattern: "api/templates"
    - from: "client/src/hooks/useSettings.ts"
      to: "/api/ai/prompt"
      via: "React Query read/update"
      pattern: "api/ai"
---

<objective>
Build the Settings page with email template management (CRUD) and AI prompt template editor.

Purpose: William can manage email templates and the AI prompt without leaving the CRM, replacing the current Google Docs workflow.
Output: Settings page with two sections: "Modeles" for email templates and "Parametres IA" for the AI prompt.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-CONTEXT.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-02-SUMMARY.md

@client/src/App.tsx
@client/src/components/layout/Sidebar.tsx
@client/src/lib/api.ts

<interfaces>
<!-- Backend API from Plan 02 -->
GET /api/templates -> [{ id, name, subject, body, variables }]
POST /api/templates { name, subject, body, variables } -> template
PUT /api/templates/:id { name?, subject?, body?, variables? } -> template
DELETE /api/templates/:id -> 204
GET /api/ai/prompt -> { promptTemplate, model }
PUT /api/ai/prompt { promptTemplate, model? } -> config
POST /api/templates/:id/preview { leadId } -> { subject, body }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with template editor and AI prompt editor</name>
  <files>client/src/pages/SettingsPage.tsx, client/src/components/settings/TemplateEditor.tsx, client/src/components/settings/AiPromptEditor.tsx, client/src/hooks/useSettings.ts, client/src/components/layout/Sidebar.tsx, client/src/App.tsx</files>
  <action>
1. Create client/src/hooks/useSettings.ts:
   - useTemplates(): useQuery GET /api/templates
   - useCreateTemplate(): useMutation POST /api/templates, invalidates templates query
   - useUpdateTemplate(): useMutation PUT /api/templates/:id, invalidates templates query
   - useDeleteTemplate(): useMutation DELETE /api/templates/:id, invalidates templates query
   - useAiPrompt(): useQuery GET /api/ai/prompt
   - useUpdateAiPrompt(): useMutation PUT /api/ai/prompt, invalidates ai prompt query

2. Create client/src/components/settings/TemplateEditor.tsx:
   - Shows list of templates on the left (or top on mobile), editor on right
   - Each template card: name, truncated subject, edit/delete buttons
   - "Nouveau modele" button opens create form
   - Edit form: name (Input), subject (Input), body (Textarea, min 6 rows), variables (display available: {{nom}}, {{date_evenement}}, {{budget}}, {{email}}, {{telephone}} as clickable chips that insert into body at cursor)
   - Save button calls create or update mutation
   - Delete button with confirmation Dialog
   - French labels: "Nom du modele", "Objet", "Corps du message", "Variables disponibles"
   - Show toast on save/delete success

3. Create client/src/components/settings/AiPromptEditor.tsx:
   - Textarea for the prompt template (min 10 rows)
   - Available variables shown as reference chips below textarea: {{nom}}, {{date_evenement}}, {{budget}}, {{email}}, {{telephone}}, {{historique_emails}}, {{notes}}
   - Model selector: Input or Select for the OpenRouter model name (default: anthropic/claude-sonnet-4)
   - "Sauvegarder" button calls useUpdateAiPrompt
   - Shows last updated timestamp
   - Toast on save success "Prompt IA sauvegarde"
   - If no prompt exists yet, show default placeholder text suggesting a starting prompt

4. Create client/src/pages/SettingsPage.tsx:
   - Tab-based layout using shadcn/ui Tabs component
   - Two tabs: "Modeles" (TemplateEditor) and "Parametres IA" (AiPromptEditor)
   - Page title: "Parametres"
   - Clean layout consistent with existing pages

5. Update Sidebar.tsx:
   - Import Settings icon from lucide-react
   - Add { to: '/settings', label: 'Parametres', icon: Settings } to NAV_ITEMS

6. Update App.tsx:
   - Import SettingsPage (lazy)
   - Add Route path="/settings" element={<SettingsPage />}
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30 && npx vite build --config client/vite.config.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Settings page accessible from sidebar, templates CRUD works (create, edit, delete with variable chips), AI prompt editor saves prompt and model, all French labels, builds clean</done>
</task>

</tasks>

<verification>
```bash
cd client && npx vite build && npx tsc --noEmit
```
</verification>

<success_criteria>
- Settings page accessible via sidebar "Parametres" link
- Template CRUD: create, edit, delete templates with name/subject/body/variables
- Variable chips clickable to insert into template body
- AI prompt editor with textarea and model selector
- Save persists to backend
- All labels in French
- Builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-05-SUMMARY.md`
</output>
