---
phase: 04-gmail-inbox-ai-drafting-and-whatsapp
plan: 07
type: execute
wave: 4
depends_on: ["04-04", "04-05", "04-06"]
files_modified:
  - src/app.ts
  - client/src/App.tsx
  - docker-compose.yml
autonomous: false
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-05
  - MAIL-06
  - MAIL-07
  - MAIL-08
  - NOTF-04
  - NOTF-05

must_haves:
  truths:
    - "Full test suite passes with no regressions"
    - "Application builds and starts cleanly with Docker Compose"
    - "All 10 Phase 4 requirements verified end-to-end"
    - "Inbox, Settings, and lead detail sections render correctly"
    - "AI draft flow enforces review before send"
  artifacts:
    - path: "src/app.ts"
      provides: "All Phase 4 routes mounted"
    - path: "client/src/App.tsx"
      provides: "All Phase 4 pages routed"
  key_links:
    - from: "client/src/pages/InboxPage.tsx"
      to: "/api/inbox/threads"
      via: "React Query"
      pattern: "useThreads"
    - from: "client/src/pages/SettingsPage.tsx"
      to: "/api/templates"
      via: "React Query CRUD"
      pattern: "useTemplates"
    - from: "client/src/components/whatsapp/WhatsAppChat.tsx"
      to: "/api/leads/:id/whatsapp"
      via: "React Query"
      pattern: "useWhatsApp"
---

<objective>
Final integration verification and end-to-end wiring for Phase 4. Ensure all routes are mounted, all pages routed, Docker Compose works with new env vars, and full test suite passes.

Purpose: Validate that all 10 requirements work together before marking Phase 4 complete.
Output: Working integrated system with visual verification.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-CONTEXT.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-04-SUMMARY.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-05-SUMMARY.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration wiring + env var config + test suite</name>
  <files>src/app.ts, docker-compose.yml, .env.example</files>
  <action>
1. Verify src/app.ts has all Phase 4 routers mounted:
   - inboxRouter at /api
   - templatesRouter at /api
   - aiRouter at /api
   - whatsappRouter at /api
   - WhatsApp webhook routes in webhook.ts
   If any are missing from earlier plans, add them now.

2. Verify client/src/App.tsx has all Phase 4 routes:
   - /inbox -> InboxPage
   - /settings -> SettingsPage
   If missing, add them.

3. Update docker-compose.yml to include new environment variables:
   - OPENROUTER_API_KEY (from .env)
   - WHATSAPP_PHONE_NUMBER_ID (from .env)
   - WHATSAPP_ACCESS_TOKEN (from .env)
   - WHATSAPP_VERIFY_TOKEN (from .env)
   - WHATSAPP_APP_SECRET (from .env)

4. Update .env.example with the 5 new env vars (with placeholder values and comments)

5. Run drizzle-kit push to apply schema changes to local DB:
   ```bash
   npx drizzle-kit push
   ```

6. Run full test suite and fix any failures:
   ```bash
   npx vitest run --reporter=verbose
   ```

7. Build production bundle and verify no errors:
   ```bash
   cd client && npx vite build
   ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20 && cd /Users/william/Documents/Development/weds-crm/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>All routes mounted, env vars configured, schema applied, full test suite green, production build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 4</name>
  <files>N/A</files>
  <action>Human verification of all Phase 4 features working end-to-end.</action>
  <what-built>Complete Phase 4: Gmail inbox with split-pane layout, email thread reading and reply, email templates with variable substitution, AI draft generation with mandatory review, WhatsApp messaging with 24h window indicator, lead detail email and WhatsApp sections, settings page</what-built>
  <how-to-verify>
    1. Start the app: `docker compose up` or `bun run dev`
    2. Navigate to "Boite de reception" in sidebar
       - Verify: split-pane layout shows Gmail threads on left
       - Click a thread: messages display on right
       - Verify: if sender matches a lead, lead badge appears on thread
    3. In thread detail, try replying:
       - Type a message in the compose area at bottom
       - Click "Envoyer" -- check in Gmail that the reply is in the same thread
    4. Try template selection:
       - First go to Settings > Modeles, create a template with {{nom}} variable
       - Return to inbox compose, select template from dropdown
       - Verify: variables are substituted with lead data in preview
    5. Test AI draft:
       - From a lead detail page, click "Generer un brouillon"
       - Verify: AI-generated text appears for review (NOT auto-sent)
       - Verify: you can edit the text before sending
    6. Navigate to Settings > Parametres IA:
       - Verify: can edit AI prompt template and model
       - Save and verify it persists on page refresh
    7. Open a lead detail page:
       - Verify: "Emails" section shows linked emails
       - Verify: "WhatsApp" section shows chat interface
       - If lead has a phone, try sending a WhatsApp message
       - Verify: 24h window indicator visible (likely "Fenetre expiree" if no inbound message)
    8. Check activity timeline includes WhatsApp events (if any messages were sent)
    9. Verify all text is in French
  </how-to-verify>
  <verify>Human visual verification</verify>
  <done>William confirms all Phase 4 features work correctly</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full test suite:
```bash
npx vitest run --reporter=verbose
```
Production build:
```bash
cd client && npx vite build
```
Docker build:
```bash
docker compose build
```
</verification>

<success_criteria>
- All 10 Phase 4 requirements verified (MAIL-01..08, NOTF-04..05)
- Full test suite passes
- Production build succeeds
- Visual verification by William confirms correct behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-07-SUMMARY.md`
</output>
