---
phase: 04-gmail-inbox-ai-drafting-and-whatsapp
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/whatsapp/WhatsAppCompose.tsx
autonomous: true
gap_closure: true
requirements:
  - NOTF-04
  - NOTF-05
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-05
  - MAIL-06
  - MAIL-07
  - MAIL-08

must_haves:
  truths:
    - "When 24h WhatsApp window is expired, free-form text input is disabled and user cannot type or send a free-form message"
    - "When 24h window is expired, template selector is the only active messaging path"
    - "When 24h window is open, free-form input works normally"
  artifacts:
    - path: "client/src/components/whatsapp/WhatsAppCompose.tsx"
      provides: "WhatsApp compose with 24h window enforcement"
      contains: "disabled.*isWindowOpen"
  key_links:
    - from: "client/src/components/whatsapp/WhatsAppCompose.tsx"
      to: "useWhatsAppWindow"
      via: "isWindowOpen state gates free-form input disabled prop"
      pattern: "disabled.*!isWindowOpen"
---

<objective>
Fix the WhatsApp 24h window enforcement gap in WhatsAppCompose.tsx.

Purpose: The free-form text input is currently always enabled when a phone number exists, regardless of whether the 24h conversation window is open. This allows users to type and attempt to send free-form messages after window expiry, which the WhatsApp API will reject. The input must be disabled when the window is expired, leaving only the template selector as the active messaging path.

Output: Updated WhatsAppCompose.tsx with proper window-based input gating.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-VERIFICATION.md

<interfaces>
From client/src/components/whatsapp/WhatsAppCompose.tsx:
```typescript
// useWhatsAppWindow returns { isOpen: boolean; expiresAt: string | null }
const { data: windowData } = useWhatsAppWindow(leadId);
const isWindowOpen = windowData?.isOpen ?? false;

// canSendMessage currently does NOT check isWindowOpen — this is the bug
const canSendMessage = hasPhone && message.trim().length > 0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate free-form WhatsApp input by 24h window state</name>
  <files>client/src/components/whatsapp/WhatsAppCompose.tsx</files>
  <action>
In WhatsAppCompose.tsx, make two changes:

1. Update `canSendMessage` (line 37) to include the window check:
   ```
   const canSendMessage = hasPhone && isWindowOpen && message.trim().length > 0;
   ```

2. Update the free-form input section (lines 159-181) to disable the Input and Send button when the window is expired:
   - Change the Input `disabled` prop from `disabled={isSending}` to `disabled={isSending || !isWindowOpen}`
   - Change the Input placeholder to show "Fenetre expiree — utilisez un modele" when `!isWindowOpen`
   - Change the Send button `disabled` prop from `disabled={!hasPhone || !message.trim() || isSending}` to `disabled={!canSendMessage || isSending}`

This ensures:
- When window is open: free-form input works normally (type + send)
- When window is expired: input is visually disabled with explanatory placeholder, send button disabled, template selector is the only active path
- The `canSendMessage` guard in `handleSend()` also prevents sending even if the disabled prop is somehow bypassed

Do NOT hide the input entirely — keep it visible but disabled so the user understands the constraint. The template selector section (lines 117-157) already correctly shows only when `!isWindowOpen`.

Update the comment on line 159 from "Free-form input — always shown when phone exists" to "Free-form input — disabled when 24h window expired".
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && grep -n "isWindowOpen" client/src/components/whatsapp/WhatsAppCompose.tsx | grep -E "disabled|canSendMessage"</automated>
  </verify>
  <done>
    - Free-form Input has `disabled={isSending || !isWindowOpen}`
    - Send button uses `disabled={!canSendMessage || isSending}` where canSendMessage includes `isWindowOpen`
    - Placeholder text indicates window expiry when disabled
    - Template selector remains the only active path when window is expired
  </done>
</task>

</tasks>

<verification>
1. `grep -n "isWindowOpen" client/src/components/whatsapp/WhatsAppCompose.tsx` shows isWindowOpen used in canSendMessage and in the Input disabled prop
2. The canSendMessage variable includes `isWindowOpen` in its condition
3. The free-form Input disabled prop references `!isWindowOpen`
</verification>

<success_criteria>
- WhatsAppCompose.tsx disables free-form input when 24h window is expired
- Template selector is the only active messaging path when window is closed
- Free-form input works normally when window is open
- No regressions to existing WhatsApp send functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-08-SUMMARY.md`
</output>
