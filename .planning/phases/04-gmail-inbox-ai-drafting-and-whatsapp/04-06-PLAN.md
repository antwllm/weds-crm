---
phase: 04-gmail-inbox-ai-drafting-and-whatsapp
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - client/src/components/whatsapp/WhatsAppChat.tsx
  - client/src/components/whatsapp/WhatsAppCompose.tsx
  - client/src/components/leads/LeadEmails.tsx
  - client/src/hooks/useWhatsApp.ts
  - client/src/hooks/useLeadEmails.ts
  - client/src/pages/LeadDetailPage.tsx
  - client/src/components/leads/ActivityTimeline.tsx
  - client/src/types/index.ts
autonomous: true
requirements:
  - MAIL-02
  - NOTF-04
  - NOTF-05

must_haves:
  truths:
    - "William can see all emails linked to a lead from the lead detail page"
    - "William can send a WhatsApp message from the lead detail page"
    - "WhatsApp conversations appear as chat bubbles in a dedicated section"
    - "24h window indicator shows when free-form messaging is available"
    - "When window expired, compose switches to template-only mode"
    - "WhatsApp messages appear in the activity timeline with WhatsApp icon"
    - "Incoming WhatsApp replies update the chat section"
  artifacts:
    - path: "client/src/components/whatsapp/WhatsAppChat.tsx"
      provides: "Chat bubble display for WhatsApp messages"
    - path: "client/src/components/whatsapp/WhatsAppCompose.tsx"
      provides: "WhatsApp text input with 24h window indicator"
    - path: "client/src/components/leads/LeadEmails.tsx"
      provides: "Email threads linked to a lead"
    - path: "client/src/pages/LeadDetailPage.tsx"
      provides: "Extended with Emails + WhatsApp sections"
  key_links:
    - from: "client/src/hooks/useWhatsApp.ts"
      to: "/api/leads/:id/whatsapp"
      via: "React Query fetch + send mutation"
      pattern: "api/leads.*whatsapp"
    - from: "client/src/hooks/useLeadEmails.ts"
      to: "/api/leads/:id/emails"
      via: "React Query fetch"
      pattern: "api/leads.*emails"
    - from: "client/src/components/leads/ActivityTimeline.tsx"
      to: "whatsapp_sent/whatsapp_received activity types"
      via: "Render new activity types"
      pattern: "whatsapp"
---

<objective>
Add WhatsApp chat UI and email section to the lead detail page. Extend the activity timeline with WhatsApp message types.

Purpose: William can communicate with prospects via WhatsApp and view all linked emails from the lead record, completing the "single interface" vision.
Output: Lead detail page extended with Emails section, WhatsApp chat section, and enhanced activity timeline.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-CONTEXT.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-02-SUMMARY.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-03-SUMMARY.md

@client/src/pages/LeadDetailPage.tsx
@client/src/components/leads/ActivityTimeline.tsx
@client/src/types/index.ts

<interfaces>
<!-- Backend APIs from Plan 02 and 03 -->
GET /api/leads/:leadId/emails -> { emails: [{ id, gmailMessageId, gmailThreadId, subject, snippet, direction, receivedAt }] }
GET /api/leads/:leadId/whatsapp -> { messages: [{ id, waMessageId, direction, body, status, createdAt }] }
POST /api/leads/:leadId/whatsapp/send { message } -> { status, waMessageId }
GET /api/leads/:leadId/whatsapp/window -> { isOpen: boolean, expiresAt: string | null }

<!-- Existing activity types in ActivityTimeline -->
Activity types rendered: email_received, sms_sent, email_sent, status_change, note_added, duplicate_inquiry, notification_failed, pipedrive_synced
New types to add: whatsapp_sent, whatsapp_received
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhatsApp chat UI + lead emails section + hooks</name>
  <files>client/src/hooks/useWhatsApp.ts, client/src/hooks/useLeadEmails.ts, client/src/components/whatsapp/WhatsAppChat.tsx, client/src/components/whatsapp/WhatsAppCompose.tsx, client/src/components/leads/LeadEmails.tsx, client/src/types/index.ts</files>
  <action>
1. Add types to client/src/types/index.ts:
   - WhatsAppMessage: { id: number; waMessageId: string; direction: 'inbound' | 'outbound'; body: string; status: string; createdAt: string }
   - WhatsAppWindow: { isOpen: boolean; expiresAt: string | null }
   - LinkedEmail: { id: number; gmailMessageId: string; gmailThreadId: string; subject: string; snippet: string; direction: string; receivedAt: string }

2. Create client/src/hooks/useWhatsApp.ts:
   - useWhatsAppMessages(leadId: number): useQuery GET /api/leads/:leadId/whatsapp, refetchInterval: 30000 (poll every 30s for new messages)
   - useSendWhatsApp(): useMutation POST /api/leads/:leadId/whatsapp/send, invalidates whatsapp messages on success
   - useWhatsAppWindow(leadId: number): useQuery GET /api/leads/:leadId/whatsapp/window

3. Create client/src/hooks/useLeadEmails.ts:
   - useLeadEmails(leadId: number): useQuery GET /api/leads/:leadId/emails

4. Create client/src/components/whatsapp/WhatsAppChat.tsx:
   - Props: leadId: number
   - Uses useWhatsAppMessages(leadId) to fetch messages
   - Displays messages as chat bubbles:
     - Outbound (direction='outbound'): right-aligned, green-tinted background (WhatsApp green), "Vous" label
     - Inbound (direction='inbound'): left-aligned, gray background, lead name label
     - Each bubble: body text, timestamp (formatDistanceToNow fr locale), status indicator for outbound (sent/delivered/read icons)
   - Scrollable container, auto-scrolls to bottom on new messages
   - If no messages, show empty state: "Aucun message WhatsApp"
   - At the bottom, render WhatsAppCompose

5. Create client/src/components/whatsapp/WhatsAppCompose.tsx:
   - Props: leadId: number, leadPhone: string | null
   - Uses useWhatsAppWindow(leadId) for 24h window status
   - Window indicator at top:
     - If isOpen: green dot + "Fenetre ouverte" + time remaining (e.g., "Expire dans 8h")
     - If not open: orange dot + "Fenetre expiree — modele requis"
   - When window is open: simple text input + Send button (MessageCircle icon)
   - When window is expired: show message "La fenetre de 24h est expiree. Utilisez un modele WhatsApp pour initier la conversation." Compose input disabled. (Note: WhatsApp Business template selection is deferred -- for V1, show the indicator and disable free-form input. Templates can be managed in Meta dashboard.)
   - If lead has no phone: show "Pas de numero de telephone" with disabled input
   - On send: call useSendWhatsApp with { leadId, message: inputText }
   - Clear input on success, show toast "Message WhatsApp envoye"
   - Show error toast on failure

6. Create client/src/components/leads/LeadEmails.tsx:
   - Props: leadId: number
   - Uses useLeadEmails(leadId)
   - Renders list of linked emails as compact cards:
     - Direction icon (ArrowDownLeft for inbound, ArrowUpRight for outbound)
     - Subject line (bold)
     - Snippet (truncated, gray text)
     - Date (relative, fr locale)
   - Clicking an email navigates to /inbox with the threadId selected (use react-router navigate with state or query param)
   - If no emails: "Aucun email lie a ce lead"
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>WhatsApp chat with bubbles, compose with 24h window indicator, lead emails list, all hooks created, TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Extend LeadDetailPage + ActivityTimeline with new sections</name>
  <files>client/src/pages/LeadDetailPage.tsx, client/src/components/leads/ActivityTimeline.tsx</files>
  <action>
1. In client/src/components/leads/ActivityTimeline.tsx:
   - Add rendering for whatsapp_sent activity type: green WhatsApp icon (MessageCircle from lucide-react in green), content text, timestamp
   - Add rendering for whatsapp_received activity type: same WhatsApp icon but in blue/incoming color, content text, timestamp
   - Update the activity type icon/color mapping to include these two new types
   - French labels: "Message WhatsApp envoye", "Message WhatsApp recu"

2. In client/src/pages/LeadDetailPage.tsx:
   - Add a tabbed or sectioned layout below the existing lead info and notes. Use shadcn/ui Tabs or vertical sections with headers.
   - Add "Emails" section: renders LeadEmails component with leadId
   - Add "WhatsApp" section: renders WhatsAppChat component with leadId
   - Keep existing sections (info, notes, activity timeline) intact
   - Section order: Lead Info -> Notes -> Emails -> WhatsApp -> Activites (timeline at bottom as full history)
   - Add "Generer un brouillon" button in the Emails section header. On click, call POST /api/ai/generate-draft { leadId }. On success, navigate to /inbox with draft text in query param or use a state management approach to pre-fill the compose window. Alternatively: show a modal with the generated draft text and a "Copier" button. This fulfills MAIL-06/MAIL-07: AI draft generated and presented for review.
   - French section headers: "Emails", "WhatsApp", "Historique d'activites"
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30 && npx vite build --config client/vite.config.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Lead detail page has Emails section with linked emails, WhatsApp section with chat bubbles and compose, AI draft button, activity timeline shows WhatsApp events, builds clean</done>
</task>

</tasks>

<verification>
Build frontend:
```bash
cd client && npx vite build
```
TypeScript check:
```bash
npx tsc --noEmit --project client/tsconfig.json
```
</verification>

<success_criteria>
- Lead detail has "Emails" section showing linked emails
- Lead detail has "WhatsApp" section with chat bubbles
- WhatsApp compose shows 24h window status indicator
- Free-form input disabled when window expired
- WhatsApp messages appear in activity timeline with green icon
- "Generer un brouillon" button available for AI drafts
- All French labels
- Frontend builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-06-SUMMARY.md`
</output>
