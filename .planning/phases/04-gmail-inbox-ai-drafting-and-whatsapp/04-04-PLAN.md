---
phase: 04-gmail-inbox-ai-drafting-and-whatsapp
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - client/src/pages/InboxPage.tsx
  - client/src/components/inbox/ThreadList.tsx
  - client/src/components/inbox/ThreadDetail.tsx
  - client/src/components/inbox/ComposeReply.tsx
  - client/src/hooks/useInbox.ts
  - client/src/components/layout/Sidebar.tsx
  - client/src/App.tsx
  - client/src/types/index.ts
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-07

must_haves:
  truths:
    - "William can see his Gmail threads in a split-pane inbox view"
    - "Clicking a thread shows the full conversation with individual messages"
    - "Unread threads appear bold in the thread list"
    - "Thread list items show sender, subject, snippet, date, and lead badge when matched"
    - "William can reply to a thread from the inline compose area"
    - "Reply appears in Gmail as part of the same conversation thread"
    - "Template dropdown in compose allows selecting and previewing a template"
    - "AI-generated draft appears in compose window for editing before sending"
  artifacts:
    - path: "client/src/pages/InboxPage.tsx"
      provides: "Split-pane inbox layout"
      min_lines: 50
    - path: "client/src/components/inbox/ThreadList.tsx"
      provides: "Thread list panel with lead badges"
    - path: "client/src/components/inbox/ThreadDetail.tsx"
      provides: "Message list display"
    - path: "client/src/components/inbox/ComposeReply.tsx"
      provides: "Reply compose with template picker + AI draft button"
  key_links:
    - from: "client/src/hooks/useInbox.ts"
      to: "/api/inbox/threads"
      via: "React Query fetch"
      pattern: "api/inbox"
    - from: "client/src/components/inbox/ComposeReply.tsx"
      to: "/api/inbox/threads/:id/reply"
      via: "POST mutation"
      pattern: "reply"
    - from: "client/src/components/inbox/ComposeReply.tsx"
      to: "/api/ai/generate-draft"
      via: "POST for AI draft"
      pattern: "generate-draft"
---

<objective>
Build the Gmail inbox UI: split-pane InboxPage with thread list and thread detail, inline reply compose with template picker and AI draft button.

Purpose: William can read and reply to emails without leaving the CRM. This is the primary differentiator of Phase 4.
Output: Full inbox page accessible via sidebar "Boite de reception" tab, with compose, template selection, and AI draft integration.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-CONTEXT.md
@.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-02-SUMMARY.md

@client/src/App.tsx
@client/src/components/layout/Sidebar.tsx
@client/src/hooks/useLeads.ts
@client/src/lib/api.ts
@client/src/types/index.ts

<interfaces>
<!-- Backend API endpoints from Plan 02 -->
GET /api/inbox/threads?pageToken=X&q=Y -> { threads: [{ id, snippet, historyId, matchedLead? }], nextPageToken }
GET /api/inbox/threads/:threadId -> { messages: [{ id, from, to, subject, date, body, messageId, inReplyTo, references }], matchedLead? }
POST /api/inbox/threads/:threadId/reply { to, subject, body, inReplyTo, references } -> { status: 'sent' }
GET /api/templates -> { templates: [{ id, name, subject, body, variables }] }
POST /api/templates/:id/preview { leadId } -> { subject, body }
POST /api/ai/generate-draft { leadId } -> { draft }
GET /api/leads/:leadId/emails -> { emails: [{ id, gmailMessageId, gmailThreadId, subject, snippet, direction, receivedAt }] }

<!-- Existing frontend patterns -->
From client/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

From client/src/components/layout/Sidebar.tsx:
```typescript
const NAV_ITEMS = [
  { to: '/pipeline', label: 'Pipeline', icon: LayoutDashboard },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inbox hooks, types, routing, and sidebar entry</name>
  <files>client/src/hooks/useInbox.ts, client/src/types/index.ts, client/src/App.tsx, client/src/components/layout/Sidebar.tsx</files>
  <action>
1. In client/src/types/index.ts, add:
   - GmailThread: { id: string; snippet: string; historyId: string; matchedLead?: { id: number; name: string; status: string } }
   - GmailMessage: { id: string; from: string; to: string; subject: string; date: string; body: string; messageId: string; inReplyTo?: string; references?: string }
   - ThreadDetail: { messages: GmailMessage[]; matchedLead?: { id: number; name: string; status: string } }
   - EmailTemplate: { id: number; name: string; subject: string; body: string; variables: string[] }
   - AiPromptConfig: { id: number; promptTemplate: string; model: string }

2. Create client/src/hooks/useInbox.ts:
   - useThreads(pageToken?: string, q?: string): useQuery for GET /api/inbox/threads
   - useThread(threadId: string | null): useQuery for GET /api/inbox/threads/:threadId, enabled only when threadId is truthy
   - useSendReply(): useMutation for POST /api/inbox/threads/:threadId/reply, invalidates thread query on success
   - useTemplates(): useQuery for GET /api/templates
   - useTemplatePreview(): useMutation for POST /api/templates/:id/preview
   - useGenerateDraft(): useMutation for POST /api/ai/generate-draft
   - useLeadEmails(leadId: number): useQuery for GET /api/leads/:leadId/emails (needed for draft-from-lead flow)

3. In client/src/components/layout/Sidebar.tsx:
   - Import Mail icon from lucide-react
   - Add { to: '/inbox', label: 'Boite de reception', icon: Mail } to NAV_ITEMS array (after Pipeline)

4. In client/src/App.tsx:
   - Import InboxPage (lazy)
   - Add Route path="/inbox" element={<InboxPage />}
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Types defined, hooks created, sidebar has "Boite de reception" entry, /inbox route registered</done>
</task>

<task type="auto">
  <name>Task 2: InboxPage split-pane + ThreadList + ThreadDetail + ComposeReply</name>
  <files>client/src/pages/InboxPage.tsx, client/src/components/inbox/ThreadList.tsx, client/src/components/inbox/ThreadDetail.tsx, client/src/components/inbox/ComposeReply.tsx</files>
  <action>
1. Create client/src/components/inbox/ThreadList.tsx:
   - Props: threads: GmailThread[], selectedId: string | null, onSelect: (id: string) => void, isLoading: boolean, onLoadMore?: () => void, hasMore: boolean
   - Render scrollable list of thread items
   - Each item shows: sender name (extract from snippet or first line), subject (if available from snippet), snippet preview (truncated), relative date (use date-fns formatDistanceToNow with fr locale)
   - Bold styling for unread threads (check if thread has UNREAD label -- if API doesn't provide this directly, bold threads that haven't been clicked in this session)
   - When matchedLead exists, show a small badge with lead name + colored status dot (reuse status colors from pipeline)
   - Selected thread has highlighted background
   - "Charger plus" button at bottom if hasMore is true
   - Use shadcn/ui Card for items, Badge for lead indicator

2. Create client/src/components/inbox/ThreadDetail.tsx:
   - Props: threadId: string
   - Uses useThread(threadId) hook
   - Shows loading skeleton while fetching
   - Renders each message in thread: sender (with avatar initial circle), date, body
   - Messages styled as card-like blocks with alternating slight background for sent vs received
   - If matchedLead exists, show link to lead detail page at the top
   - At the bottom, render ComposeReply component

3. Create client/src/components/inbox/ComposeReply.tsx:
   - Props: threadId: string, lastMessage: GmailMessage (to extract reply-to info), leadId?: number, initialDraft?: string
   - State: body text (initialized from initialDraft if provided), isSubmitting, selectedTemplateId, showAiDraft
   - Subject field: pre-filled with "Re: {lastMessage.subject}" (read-only for replies)
   - To field: pre-filled with lastMessage.from (editable)
   - Body textarea: main compose area, min 4 rows, auto-expands
   - Template picker: dropdown (Select component) listing templates from useTemplates(). On select, call useTemplatePreview with template id + leadId. Show preview modal/popover with substituted content. "Inserer" button copies subject+body into compose fields.
   - AI draft button: "Generer un brouillon" (Sparkles icon from lucide-react). Only visible when leadId is present. On click, call useGenerateDraft({ leadId }). Show loading spinner. On success, insert returned draft text into body textarea. User can then edit before sending. CRITICAL: No auto-send. The draft populates the textarea, user must click "Envoyer" manually.
   - Send button: "Envoyer" (Send icon). Calls useSendReply with { threadId, to, subject: replySubject, body, inReplyTo: lastMessage.messageId, references: (lastMessage.references || '') + ' ' + lastMessage.messageId }
   - Show toast (sonner) on send success "Reponse envoyee" or error "Erreur lors de l'envoi"
   - After successful send, clear body textarea

4. Create client/src/pages/InboxPage.tsx:
   - Split-pane layout using CSS grid: left panel (thread list, ~350px on desktop, full width on mobile), right panel (thread detail, flex-1)
   - State: selectedThreadId, pageToken
   - Left panel: ThreadList with threads from useThreads()
   - Right panel: if selectedThreadId, show ThreadDetail; else show empty state "Selectionnez un fil de discussion"
   - Mobile responsive: on small screens, show only thread list OR thread detail (not both). Back button to return to list.
   - Use Tailwind grid: `grid grid-cols-1 md:grid-cols-[350px_1fr]` with overflow handling
   - Handle location.state for draft-from-lead flow (Plan 06 navigates here with { draft, leadId, leadEmail }):
     a) On mount, read react-router location.state. If state contains { draft, leadId }:
     b) Query the lead's linked emails via GET /api/leads/:leadId/emails (use useLeadEmails hook).
     c) If linked emails exist, extract gmailThreadId from the most recent email and auto-select that thread (set selectedThreadId). Pass the draft text to ComposeReply via initialDraft prop so the body textarea is pre-filled.
     d) If NO linked emails exist for this lead (no prior email thread), show a standalone compose view: render ComposeReply in "new email" mode (no threadId). Populate the To field from leadEmail in location.state, leave subject empty, pre-fill body with the draft text. This prevents the draft from being consumed and lost when no thread exists for the lead.
     e) Clear the location state after consuming it (use window.history.replaceState to replace current history entry without state) to prevent stale drafts on browser refresh.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30 && npx vite build --config client/vite.config.ts 2>&1 | tail -5</automated>
  </verify>
  <done>InboxPage renders split-pane layout, thread list shows threads with lead badges, thread detail shows messages, compose reply works with template picker and AI draft button, draft-from-lead flow works both with and without existing threads, all French labels, builds without errors</done>
</task>

</tasks>

<verification>
Build the frontend:
```bash
cd client && npx vite build
```
TypeScript check:
```bash
npx tsc --noEmit --project client/tsconfig.json
```
</verification>

<success_criteria>
- Sidebar shows "Boite de reception" tab navigating to /inbox
- Split-pane layout: thread list on left, detail on right
- Thread list items show sender, snippet, date, lead badge
- Clicking thread shows full conversation messages
- Compose area docked at bottom of thread detail
- Template dropdown works with preview substitution
- AI draft button generates text into compose area (no auto-send)
- Send button sends reply with correct threading headers
- Draft-from-lead flow auto-selects thread or shows standalone compose
- Mobile responsive (single column)
- All French labels
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-inbox-ai-drafting-and-whatsapp/04-04-SUMMARY.md`
</output>
