---
phase: 01-foundation-and-automation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - drizzle.config.ts
  - Dockerfile
  - .dockerignore
  - .env.example
  - src/config.ts
  - src/types.ts
  - src/logger.ts
  - src/db/schema.ts
  - src/db/index.ts
  - tests/helpers/fixtures.ts
autonomous: true
requirements:
  - INFR-02
  - INFR-04

must_haves:
  truths:
    - "TypeScript compiles without errors"
    - "Database schema covers all 4 phases (leads, activities, sync_log, email_templates, linked_emails)"
    - "Docker image builds successfully"
    - "Vitest runs and finds test files"
    - "Environment configuration validates required variables"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Full Drizzle schema for all 4 phases"
      contains: "leads, activities, syncLog, emailTemplates, linkedEmails"
    - path: "src/config.ts"
      provides: "Centralized env var access with zod validation"
      exports: ["config"]
    - path: "src/types.ts"
      provides: "Shared TypeScript types (ParsedLead, NotificationResult, etc.)"
      exports: ["ParsedLead", "NotificationResult"]
    - path: "src/logger.ts"
      provides: "Structured logging with Sentry integration"
      exports: ["logger"]
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
    - path: "Dockerfile"
      provides: "Multi-stage Docker build for Cloud Run"
    - path: "tests/helpers/fixtures.ts"
      provides: "Sample Mariages.net email bodies for tests"
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/schema.ts"
      via: "drizzle() initialization with schema"
      pattern: "drizzle.*schema"
    - from: "src/config.ts"
      to: "zod"
      via: "Runtime validation of process.env"
      pattern: "z\\.object"
---

<objective>
Project scaffolding, database schema, configuration, and test infrastructure for the Weds CRM backend.

Purpose: Every subsequent plan depends on this foundation -- schema types, config, logging, and test fixtures must exist first.
Output: A buildable TypeScript project with full Drizzle schema, Docker support, Vitest configured, and test fixtures ready.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-automation-core/01-CONTEXT.md
@.planning/phases/01-foundation-and-automation-core/01-RESEARCH.md
@.planning/phases/01-foundation-and-automation-core/01-VALIDATION.md

Reference existing code for porting patterns:
@/Users/william/Documents/Development/email-parser/src/config.js
@/Users/william/Documents/Development/email-parser/src/utils/parser.js
@/Users/william/Documents/Development/email-parser/src/utils/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with TypeScript, dependencies, and build tooling</name>
  <files>package.json, tsconfig.json, .env.example, .dockerignore, Dockerfile, drizzle.config.ts</files>
  <action>
1. Run `npm init -y` and install all dependencies from RESEARCH.md:
   - Production: typescript, express, @types/express, drizzle-orm, pg, @types/pg, @google-cloud/pubsub, @google-cloud/storage, googleapis, @sentry/node, passport, passport-google-oauth20, @types/passport-google-oauth20, express-session, @types/express-session, connect-pg-simple, @types/connect-pg-simple, node-cron, @types/node-cron, axios, zod, dotenv
   - Dev: drizzle-kit, tsx, @types/node, vitest, @vitest/coverage-v8

2. Create `tsconfig.json`: target ES2022, module NodeNext, moduleResolution NodeNext, outDir dist, rootDir src, strict true, esModuleInterop true, skipLibCheck true, resolveJsonModule true.

3. Create `.env.example` with ALL required env vars (documented with comments in French where relevant):
   - DATABASE_URL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI, SESSION_SECRET
   - TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER
   - FREE_MOBILE_USER, FREE_MOBILE_PASS
   - GCS_BUCKET_NAME, SENTRY_DSN, GMAIL_PUBSUB_TOPIC
   - ADMIN_EMAIL (contact@weds.fr), ALLOWED_USER_EMAIL (William's Google email)
   - NODE_ENV, PORT

4. Create `Dockerfile`: multi-stage build (builder stage compiles TS, production stage copies dist + node_modules with --omit=dev). Expose PORT 8080 (Cloud Run default). CMD ["node", "dist/index.js"].

5. Create `.dockerignore`: node_modules, .env, dist, tests, .planning, .git.

6. Create `drizzle.config.ts`: schema path src/db/schema.ts, out src/db/migrations, driver pg, dbCredentials from DATABASE_URL env var.

7. Add npm scripts to package.json: "build": "tsc", "dev": "tsx watch src/index.ts", "start": "node dist/index.js", "db:push": "drizzle-kit push", "db:generate": "drizzle-kit generate", "test": "vitest run --reporter=verbose", "test:coverage": "vitest run --coverage".
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --pretty 2>&1 | head -5; echo "---"; npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript compiles (possibly with empty src), npm test runs vitest without errors, Docker builds, all dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create database schema, types, config, and logger</name>
  <files>src/db/schema.ts, src/db/index.ts, src/config.ts, src/types.ts, src/logger.ts</files>
  <action>
1. Create `src/db/schema.ts` with the FULL Drizzle schema from RESEARCH.md covering all 4 phases:
   - `leadStatusEnum`: nouveau, contacte, rdv, devis_envoye, signe, perdu
   - `leads` table: id (serial PK), name, email, phone (E.164), eventDate, message, source (default 'mariages.net'), status (default 'nouveau'), vCardUrl, gmailMessageId, pipedrivePersonId (Phase 3), pipedriveDealId (Phase 3), createdAt, updatedAt
   - `activityTypeEnum`: email_received, sms_sent, email_sent, status_change, note_added, duplicate_inquiry, notification_failed, pipedrive_synced
   - `activities` table: id, leadId (FK to leads), type, content, metadata (jsonb), gmailMessageId, createdAt
   - `syncLog` table (Phase 3): id, leadId (FK), direction, status, error, createdAt
   - `emailTemplates` table (Phase 4): id, name, subject, body, variables (jsonb), createdAt, updatedAt
   - `linkedEmails` table (Phase 4): id, leadId (FK), gmailMessageId, subject, snippet, direction, receivedAt, createdAt
   - Export ALL tables and enums

2. Create `src/db/index.ts`: initialize drizzle with pg Pool using DATABASE_URL from config. Export `db` instance. Use lazy initialization pattern so tests can import schema types without connecting.

3. Create `src/config.ts`: use zod to validate ALL environment variables. Separate required (DATABASE_URL, session secret, Google OAuth) from optional (SENTRY_DSN). Export a typed `config` object. Include sensible defaults for PORT (8080), NODE_ENV ('development'), VCARD_EXPIRY_DAYS (7), FALLBACK_SWEEP_INTERVAL ('*/30 * * * *'). All notification-related vars (Twilio, Free Mobile, GCS) should be required in production but optional in development/test.

4. Create `src/types.ts`: define and export shared types:
   - `ParsedLead`: { name, email, phone, eventDate, message, rawBody }
   - `NotificationResult`: { channel: string, success: boolean, error?: string }
   - `PipelineResult`: { leadId: number, isDuplicate: boolean, notifications: NotificationResult[] }
   - Re-export relevant Drizzle inferred types (Lead, Activity, etc.) using `typeof leads.$inferSelect`

5. Create `src/logger.ts`: structured logger that wraps console.log/error/warn with timestamps and JSON format. Include Sentry integration: if SENTRY_DSN is set, call `Sentry.captureException` on error-level logs. Export `logger` object with info, warn, error, debug methods. Each method accepts (message: string, context?: Record<string, unknown>).

All text in French where user-facing (error messages in logs can be English for developer consumption).
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles cleanly. Schema exports all tables/enums. Config validates env vars with zod. Types are importable. Logger provides structured output.</done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest config and test fixtures with sample Mariages.net emails</name>
  <files>vitest.config.ts, tests/helpers/fixtures.ts, tests/helpers/db.ts</files>
  <action>
1. Create `vitest.config.ts`: configure Vitest with TypeScript support, test include pattern `tests/**/*.test.ts`, root as project root, globals false (explicit imports), coverage provider v8.

2. Create `tests/helpers/fixtures.ts`: export sample Mariages.net email bodies for use in parser tests. Include:
   - `VALID_MARIAGES_EMAIL`: a complete, realistic Mariages.net inquiry email body with name, date, email, phone, and message. Use the regex patterns from email-parser/src/utils/parser.js to ensure the fixtures match the format: "L'utilisateur [name] s'est...", "DATE EVENEMENT: ...", "E-MAIL: ...", "TELEPHONE : ...", message block between TELEPHONE and "Repondez".
   - `VALID_MARIAGES_EMAIL_NO_PHONE`: same format but phone field empty/missing
   - `VALID_MARIAGES_EMAIL_DUPLICATE`: same email/phone as VALID_MARIAGES_EMAIL (for duplicate detection tests)
   - `INVALID_EMAIL_BODY`: non-Mariages.net email body (should fail parsing)
   - `EXPECTED_PARSED_LEAD`: the expected ParsedLead output for VALID_MARIAGES_EMAIL

3. Create `tests/helpers/db.ts`: a test database helper that:
   - Exports a `setupTestDb()` function that creates a drizzle instance pointing at a test database (DATABASE_URL from env or defaults to a local test DB)
   - Exports a `cleanupTestDb()` function that truncates all tables
   - These will be used by integration tests in later plans

4. Verify vitest discovers test helper files and can import from src/ without path issues.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest runs successfully. Fixtures file exports all sample data. Test helpers compile. TypeScript has no errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vitest run --passWithNoTests` completes successfully
- `docker build -t weds-crm .` completes (may fail at runtime without env vars, but build succeeds)
- All exports from schema.ts, config.ts, types.ts, logger.ts resolve correctly
</verification>

<success_criteria>
- TypeScript project compiles cleanly with all dependencies installed
- Database schema defines tables for all 4 phases (leads, activities, syncLog, emailTemplates, linkedEmails)
- Config validates environment variables with zod and provides typed access
- Shared types (ParsedLead, NotificationResult, PipelineResult) are defined and exported
- Logger integrates with Sentry when DSN is available
- Dockerfile builds successfully with multi-stage build
- Vitest configured and runnable
- Test fixtures contain realistic Mariages.net email samples matching parser regex patterns
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-automation-core/01-01-SUMMARY.md`
</output>
