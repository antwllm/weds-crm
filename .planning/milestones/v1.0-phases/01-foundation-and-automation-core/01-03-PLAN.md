---
phase: 01-foundation-and-automation-core
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/services/parser.ts
  - src/services/gmail.ts
  - src/services/vcard.ts
  - src/services/storage.ts
  - tests/parser.test.ts
  - tests/vcard.test.ts
  - tests/gmail.test.ts
  - tests/storage.test.ts
autonomous: true
requirements:
  - PARS-01
  - PARS-03
  - PARS-04
  - LEAD-11

must_haves:
  truths:
    - "Mariages.net email body is parsed into structured lead data (name, email, phone, eventDate, message)"
    - "Phone numbers are normalized to E.164 French format"
    - "Unparseable emails return a clear error (not a crash)"
    - "Gmail messages can be searched by label, read, and relabeled"
    - "vCard content is generated in VCF 3.0 format with correct encoding"
    - "vCard is uploaded to GCS and a signed URL is returned"
  artifacts:
    - path: "src/services/parser.ts"
      provides: "Mariages.net email parsing with regex extraction and phone normalization"
      exports: ["parseMarriagesNetEmail", "normalizePhoneNumber"]
    - path: "src/services/gmail.ts"
      provides: "Gmail API wrapper: search, read, modify labels, send email, manage labels"
      exports: ["searchMessages", "getMessageContent", "modifyLabels", "sendEmail", "ensureLabelsExist", "getGmailClient"]
    - path: "src/services/vcard.ts"
      provides: "vCard content generation in VCF 3.0 format"
      exports: ["generateVCardContent"]
    - path: "src/services/storage.ts"
      provides: "GCS upload and signed URL generation"
      exports: ["uploadVCardAndGetSignedUrl"]
  key_links:
    - from: "src/services/parser.ts"
      to: "src/types.ts"
      via: "Returns ParsedLead type"
      pattern: "ParsedLead"
    - from: "src/services/gmail.ts"
      to: "googleapis"
      via: "google.gmail({ version: 'v1' })"
      pattern: "google\\.gmail"
    - from: "src/services/storage.ts"
      to: "@google-cloud/storage"
      via: "Storage client for GCS bucket"
      pattern: "new Storage"
---

<objective>
Email parsing, Gmail API service, vCard generation, and Cloud Storage upload -- the data extraction and asset generation layer.

Purpose: These are the pure-function and service layers that the pipeline orchestrator (Plan 04) will compose. Parser and vCard are strong TDD candidates with defined inputs and outputs. Gmail and Storage services wrap external APIs and are tested with mocked clients.
Output: Tested parser, Gmail service, vCard generator, and Cloud Storage uploader.
</objective>

<execution_context>
@/Users/william/.claude/get-shit-done/workflows/execute-plan.md
@/Users/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-automation-core/01-CONTEXT.md
@.planning/phases/01-foundation-and-automation-core/01-RESEARCH.md
@.planning/phases/01-foundation-and-automation-core/01-01-SUMMARY.md

Port from existing code:
@/Users/william/Documents/Development/email-parser/src/utils/parser.js
@/Users/william/Documents/Development/email-parser/src/utils/helpers.js
@/Users/william/Documents/Development/email-parser/src/services/google.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD parser and phone normalization</name>
  <files>tests/parser.test.ts, src/services/parser.ts</files>
  <behavior>
    - parseMarriagesNetEmail(validBody) returns ParsedLead with name, email, phone, eventDate, message
    - parseMarriagesNetEmail(validBodyNoPhone) returns ParsedLead with phone as null
    - parseMarriagesNetEmail(invalidBody) returns null or throws descriptive error
    - normalizePhoneNumber("06 12 34 56 78") returns "+33612345678"
    - normalizePhoneNumber("0033612345678") returns "+33612345678"
    - normalizePhoneNumber(null) returns null
    - normalizePhoneNumber("invalid") returns null
    - Parser extracts message body from between TELEPHONE line and "Repondez" marker
  </behavior>
  <action>
RED phase:
1. Create `tests/parser.test.ts` with tests for all behaviors above using fixtures from tests/helpers/fixtures.ts.
2. Import ParsedLead from src/types.ts. Tests should verify exact field values against EXPECTED_PARSED_LEAD fixture.
3. Run tests -- they must FAIL (module not found).

GREEN phase:
4. Create `src/services/parser.ts`:
   - Port regex patterns from email-parser/src/utils/parser.js:
     * nameRegex: /L'utilisateur\s+(.*?)\s+s'est/i
     * eventDateRegex: /DATE EV[EE]NEMENT:\s*(.*)/i
     * emailRegex: /(?:E-MAIL|EMAIL):\s*(.*)/i
     * phoneRegex: /TELEPHONE\s*:\s*(.*)/i
     * messageBlockRegex: /(TELEPHONE\s*:[\s\S]*?)(?=\sRepondez)/i
   - `parseMarriagesNetEmail(body: string): ParsedLead | null` -- returns null if name or email cannot be extracted
   - Port `normalizePhoneNumber(phone: string | null): string | null` from email-parser/src/utils/helpers.js
   - Trim all extracted values, normalize phone with normalizePhoneNumber
   - Extract message from messageBlock (strip the TELEPHONE line itself)
5. Run tests -- they must PASS.

REFACTOR phase:
6. Clean up if needed. Ensure all exports are typed.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run tests/parser.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All parser tests pass. Regex patterns correctly extract Mariages.net fields. Phone normalization handles French numbers. Invalid input returns null gracefully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD vCard generation, Gmail service, and Cloud Storage upload with tests</name>
  <files>tests/vcard.test.ts, tests/gmail.test.ts, tests/storage.test.ts, src/services/vcard.ts, src/services/storage.ts, src/services/gmail.ts</files>
  <behavior>
    - generateVCardContent({ name, email, phone, eventDate }) returns valid VCF 3.0 string
    - vCard contains BEGIN:VCARD, VERSION:3.0, FN, TEL, EMAIL, NOTE, END:VCARD
    - vCard handles missing phone (omits TEL line)
    - vCard handles special characters in name (accents)
    - getGmailClient returns authenticated Gmail client
    - searchMessages calls gmail.users.messages.list with correct labelIds
    - getMessageContent decodes base64 message body
    - modifyLabels calls gmail.users.messages.modify with correct add/remove label IDs
    - ensureLabelsExist creates missing labels and caches results
    - sendEmail constructs raw MIME message and calls gmail.users.messages.send
    - uploadVCardAndGetSignedUrl uploads to GCS bucket and returns signed URL with configured expiry
    - uploadVCardAndGetSignedUrl uses sanitized filename pattern vcards/{timestamp}-{name}.vcf
  </behavior>
  <action>
RED phase:
1. Create `tests/vcard.test.ts` with tests for vCard generation behaviors above.

2. Create `tests/gmail.test.ts`:
   - Mock googleapis at module level using vi.mock('googleapis')
   - Create a mock Gmail client object with mocked users.messages.list, users.messages.get, users.messages.modify, users.messages.send, users.labels.list, users.labels.create
   - Test getGmailClient: verify it creates an OAuth2 client and returns gmail instance
   - Test searchMessages: verify it calls users.messages.list with the provided labelId, returns message array
   - Test getMessageContent: verify it calls users.messages.get, decodes base64url body from payload.parts or payload.body
   - Test modifyLabels: verify it calls users.messages.modify with addLabelIds and removeLabelIds
   - Test ensureLabelsExist: verify it lists existing labels, creates missing ones (weds-crm/pending, weds-crm/processed, weds-crm/error), returns name->id map
   - Test ensureLabelsExist caching: second call returns cached result without API call
   - Test sendEmail: verify it constructs raw MIME with correct headers, calls users.messages.send

3. Create `tests/storage.test.ts`:
   - Mock @google-cloud/storage at module level using vi.mock('@google-cloud/storage')
   - Create mock Storage instance with bucket().file().save() and bucket().file().getSignedUrl()
   - Test uploadVCardAndGetSignedUrl: verify it calls file.save with vCard content, then getSignedUrl with correct expiry
   - Test uploadVCardAndGetSignedUrl: verify file path matches pattern vcards/{timestamp}-{sanitized-name}.vcf
   - Test uploadVCardAndGetSignedUrl: verify it returns the signed URL string
   - Test uploadVCardAndGetSignedUrl: verify special characters in name are sanitized in filename

4. Run all tests -- must FAIL.

GREEN phase:
5. Create `src/services/vcard.ts`:
   - Port `createVCardContent` from email-parser/src/utils/helpers.js to TypeScript
   - `generateVCardContent(lead: { name: string, email?: string | null, phone?: string | null, eventDate?: string | null }): string`
   - VCF 3.0 format with proper line endings (\r\n)
   - Include NOTE field with event date if provided
   - Handle null/missing fields gracefully (omit the line)

6. Create `src/services/gmail.ts`:
   - `getGmailClient(accessToken: string, refreshToken: string): gmail_v1.Gmail` -- creates authenticated Gmail client using OAuth2
   - `ensureLabelsExist(gmail: gmail_v1.Gmail): Promise<Record<string, string>>` -- checks for weds-crm/pending, weds-crm/processed, weds-crm/error labels. Creates them if missing. Returns map of name -> labelId. Cache the result in module scope.
   - `searchMessages(gmail: gmail_v1.Gmail, labelId: string): Promise<gmail_v1.Schema$Message[]>` -- searches for messages with given label ID
   - `getMessageContent(gmail: gmail_v1.Gmail, messageId: string): Promise<string>` -- gets the decoded text/plain or text/html body of a message
   - `modifyLabels(gmail: gmail_v1.Gmail, messageId: string, addLabelIds: string[], removeLabelIds: string[]): Promise<void>` -- adds/removes labels on a message
   - `sendEmail(gmail: gmail_v1.Gmail, to: string, subject: string, body: string, attachments?: { filename: string, content: string, mimeType: string }[]): Promise<void>` -- sends email with optional attachments using raw MIME construction. Port MIME building from email-parser/src/services/google.js.
   - All functions accept the gmail client as first arg for testability (dependency injection)

7. Create `src/services/storage.ts`:
   - `uploadVCardAndGetSignedUrl(name: string, vCardContent: string, expiryDays?: number): Promise<string>`
   - Uses @google-cloud/storage to upload to GCS_BUCKET_NAME
   - File path: `vcards/${Date.now()}-${sanitized-name}.vcf`
   - Returns signed URL with configurable expiry (default from config.vCardExpiryDays)
   - Logs upload success with file path and expiry

8. Run all tests -- must PASS.
  </action>
  <verify>
    <automated>cd /Users/william/Documents/Development/weds-crm && npx vitest run tests/vcard.test.ts tests/gmail.test.ts tests/storage.test.ts tests/parser.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>vCard tests pass. Gmail service tests pass with mocked googleapis. Storage service tests pass with mocked @google-cloud/storage. All TypeScript clean.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/parser.test.ts` -- all parser tests green
- `npx vitest run tests/vcard.test.ts` -- all vCard tests green
- `npx vitest run tests/gmail.test.ts` -- all Gmail service tests green
- `npx vitest run tests/storage.test.ts` -- all Storage service tests green
- `npx tsc --noEmit` -- zero errors
- Parser regex matches real Mariages.net email format from fixtures
- Phone normalization handles "06...", "0033...", "+33...", null, invalid
</verification>

<success_criteria>
- Parser extracts all fields from Mariages.net email format with proven regex patterns
- Phone normalization converts French numbers to E.164 (+33...)
- vCard generates valid VCF 3.0 content with proper encoding
- Gmail service provides search, read, modify, send, and label management -- all tested with mocked googleapis
- Storage service uploads to GCS and returns signed URLs -- tested with mocked @google-cloud/storage
- All services accept injected dependencies (gmail client, storage client) for testability
- TDD commit history: failing tests -> passing implementation -> refactor
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-automation-core/01-03-SUMMARY.md`
</output>
